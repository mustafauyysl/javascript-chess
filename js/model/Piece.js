class Piece {
    constructor(color, type, point,startLocations) {
        this.color = color;
        this.type = type;
        this.point = point;
        this.startLocations = startLocations
    }

    
}